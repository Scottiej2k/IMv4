# Scripts

Build and validation scripts (schema check, lexicon coverage, output generation), the writing
pipeline, `update_logs.py` (rebuilds the lexicon and continuity log) and `run_batch.py` (several
chapters at once). Each script's docstring explains its use.
